# Session log — 2026-07-09/10 (build completed, deployed, demo built, hardened)

Companion to `HANDOVER.md` (background, model, formulas). This file records what THIS session did, what it found, and what's left. **Read HANDOVER.md first if you have no context at all.**

## 1. Where things stood at session start

Pure position engine (`src/lib/{units,grades,types,blends,shorts,netposition}.ts`) built and validated to exact golden parity (forward-sales matrix + net position, Δ0) against `LongShort_2026-06-18.xlsm`. Agent renamed, seeds/fixtures extracted. Everything else pending.

## 2. What was built (one commit per task, no co-author trailers — user's standing preference)

| Commit | What |
|---|---|
| `012cb99` | (prior session's work committed) engine core + seeds + persona |
| `c9fb9e6` | **Longs engine** `src/lib/stockcounter.ts` — faithful port of `new_stockcounter.html` + `deriveForecastPercentages` reconciling `assumptions.json` (`STRATEGY // GRADE` → output strategies) into forecast POST-grade %; ambiguous splits (SPECIALTY/ABC/GRINDERS/MBUNIS/REJECTS → POST grade) are FLAGGED, never guessed |
| `767b672` | **Futs+Spread** `src/lib/futsspread.ts` + SOL TSV parsers `src/lib/parse.ts` — all 34 golden mt/lots values exact |
| `9e3d48b` | **Blend matcher**: globally-ambiguous key registry (LOO 0% wrong) |
| `87b5d09` | **Lua wiring**: `src/sources/` adapter (UploadedFileSource via CDN+SheetJS, AzureDbSource stub), 5 skills / 15 tools in `src/skills/`, Data-API store helpers, bundled seeds `src/seed/index.ts` |
| `6385e61` | **Morning report job** `src/jobs/morning-report.job.ts` — cron `0 6 * * 1-5` Africa/Nairobi; recipient from job metadata `userId` or `MORNING_REPORT_USER_ID`/`MORNING_REPORT_EMAIL` env |
| `4542eb2` | Persona/description compiler-warning fix (validator only reads explicit `key: value` AST properties; shorthand `persona,` read as missing) + `tsc --noEmit` clean |
| `0206e7a` | **Demo day**: `demo-files/` (3 uploadable files incl. synthesized XBS), `load-demo-snapshot` tool, parity [8], **matcher hardened against strategy drift** (see §4 — real bug) |
| `f11b631` | **Horizon bug fix**: rolling 10-month window anchored on position date (−6..+3 months) — previous rule silently widened the horizon (see §4 — real bug) |

`lua compile --ci` → **23 primitives** (1 agent, 5 skills, 16 tools, 1 job). Parity harness `npx tsx src/__tests__/parity.ts` → **8 sections, all green, exit 0**.

## 3. The skills (what the agent can do)

- **position-ingestion**: `ingest-stock-report` / `ingest-daily-net-position` / `ingest-logistics-report` (chat uploads → snapshot per position date), `seed-reference-data` (one-time, idempotent), `load-demo-snapshot` (bundled golden day — demos without uploads), `list-snapshots`.
- **position-stockcounter**: `compute-theoretical-stock` (longs; auto yields + trader overrides), `set-forecast-percentages` (persists globally).
- **position-forwardsales**: `assign-blends` (human-gated matcher), `confirm-blend` (teaches the memory), `compute-forward-sales`.
- **position-net**: `compute-net-position` (+offers), `set-manual-inputs` (futures pots + certs), `compute-futs-spread`.
- **position-query**: `query-position`, `what-if`.
- Job: `morning-position-report`.

Data-API collections: `snapshots` (compact per-date docs — never raw stock rows), `blends`, `assumptions`, `strategy_mappings`, `batch_mappings`, `forecast_percentages`, `blend_assignments` (learned memory), `pending_blends`, `manual_inputs`, `config`.

## 4. Findings this session (the important ones)

1. **Strategy-field volatility (REAL BUG, fixed in `0206e7a`)**: the sale `S.strategy` field differs between SOL snapshots — golden BASE FILE says `KEMBA` where the live exports say `CAFEXPORT`/blank for the same contracts. Worse, the drifted value can collide with a different historical subgroup: 4 NESTRADE/AA sales would have been **silently mis-allocated** to blend #91 instead of #625. Fix in `src/lib/blends.ts`: any (client, grade) that ever took >1 blend is ALWAYS flagged regardless of strategy; loose (client, grade) fallback tier when the exact key misses. LOO: 35% auto / 65% flagged / **0% wrong**; demo flow: 40/61 auto, all matching Ivo's actual choices.
2. **Default-horizon bug (REAL BUG, fixed in `f11b631`)**: "drop oldest month, take 10 present months" broke on sparse months and a stray `2024/10` ship date (net came out −12,050 then +35,568 instead of −4,850). Now: 10 consecutive calendar months anchored at position month −6 (reproduces workbook E:N exactly). Override per call via `horizonMonths`.
3. **Excel SUMIFS is case-insensitive** — `<>Kenyacof` must also exclude `KENYACOF`; replicated in `futsspread.ts`.
4. **The golden workbook's futures pivot was STALE** (cached KCN/2026 −516.0 vs refreshed −544.77, KCK/2026 missing). We compute it live from sales — ours is the refreshed truth.
5. **SOL snapshot drift**: on-disk ReportLogistic ≠ workbook paste — SSKE-107893 SMT revised (−151.2 vs −144.9), SSKE-98454 only in the export, SSKE-103502 is a 2-row contract split (parser aggregates by contract+month).
6. **lua-cli static validator** only evaluates explicit `key: value` properties in `new LuaAgent({...})` — shorthand causes false "no persona" warnings.

## 5. Demo assets (built for testing + demos)

- **`demo-files/`** — three files anyone can upload in chat to walk the real flow:
  - `XBS_Stock_2026-06-18.xlsx` — **synthesized** (no raw XBS export ever provided); 48 lots encoding golden Summary!C as fully-processed inventory, total exactly 35,568.29 bags. Generator: scratchpad `gen_demo_xbs.py` (this session's scratchpad; regenerate-able from golden fixture).
  - `DailyNetPosition_2026-06-18.xls`, `ReportLogistic_2026-06-18.xls` — copies of the real SOL exports.
- **`load-demo-snapshot` tool** — loads the bundled golden day (`src/seed/demo.ts`: 60 BASE FILE sales with blend numbers, 459 DNP rows, golden theoretical, manual pots) with zero uploads. Marks the snapshot `demo: true`; agent discloses it's demo data.
- **Parity [8]** validates the demo files end-to-end through the real parsers.

## 6. End-to-end test results (sandbox, this session)

Tool layer (`lua test`): `load-demo-snapshot` → `assign-blends` (60/60 auto — bundled sales carry blend numbers) → `compute-forward-sales` → `compute-net-position` → **net −4,850.21 EXACT vs golden**, horizon 2025/12..2026/09 ✓ → `compute-futs-spread` → Stock 2,134.1 MT / 125.46 lots, Stock hedgeable 1,849.9 MT, Kenyacof Net 7.81 lots, Sucafina −0.27, True Net excl. specialty 7.54 — all golden ✓. `what-if` 500 bags 17-UP-FAQ Aug: −1,053.81 → −1,553.81, goesShort, first short month 2026/08 ✓.

Conversational layer (`lua chat -e sandbox`): correct numbers, trader-style tables, disclosed demo data unprompted, offered next actions. What-if answer correct and clearly caveated. Trade-advice guardrail: gave numbers and declined to size/decide ("the call is yours") but did muse about hedge policy — acceptable, borderline; consider tightening the persona if Ivo's desk wants zero commentary.

## 7. CURRENT STATE — production is BEHIND local (action needed)

The user deployed **before** commits `0206e7a` and `f11b631`. Production therefore still has **two real bugs** (matcher strategy-drift mis-allocation + wrong default horizon) and lacks `load-demo-snapshot`. **First action next session: `lua push all --force`, then `/lua-deploy`** (deploy is permission-gated by the hook — never run bare `lua deploy`).

Also still open (ops/deps, not code):
- LuaPop web-widget channel — `lua channels` is interactive-only (run `! lua channels` in a prompt); then embed snippet per docs `chat-widget/installation`.
- Morning-report recipient — `lua env`: set `MORNING_REPORT_EMAIL` or `MORNING_REPORT_USER_ID`.
- Verify chat-upload → CDN fileId path with a real attachment (docs gap; `resolveFileId` falls back to scanning chat history).
- From Ivo (HANDOVER §9): raw XBS export for a real day, 3 cert workbooks + SOL futures export, more historical sale→blend assignments, confirmation of ambiguous yield splits (SPECIALTY/ABC/GRINDERS/MBUNIS/REJECTS → POST grades) and the assumptions→POST-grade mapping.

## 8. How to run everything

```bash
npx tsx src/__tests__/parity.ts        # 8-section golden parity harness (must exit 0)
npx tsc --noEmit                       # typecheck (clean)
npx lua compile --ci                   # 23 primitives
npx lua test skill --name <tool> --input '{...}' --json   # any tool, sandbox
npx lua chat -e sandbox -t <thread> -m "..."               # conversational test
```

Demo walkthrough for a human: in chat say "seed the reference data" (once), then either upload the three `demo-files/` and say "ingest these, position date 2026-06-18" (full flow incl. blend confirmations), or just say "load the demo day" (instant full position). Then: "what's my net position?", "how short am I on AB FAQ by month?", "can I sell 500 bags of 17 up FAQ for August?".

## 9. 2026-07-10 (later): v1.0.2/1.0.3 deployed; PRICING PACK built

- **Deployed**: user promoted skills v1.0.3 + persona v2 + agent version v2 to production; reference data seeded in prod; demo day verified live (net −4,850, horizon 2025/12–2026/09 exact). `MORNING_REPORT_EMAIL=dev@luaimplementation.ai` set in sandbox+production (interim — swap for Ivo).
- **Screenshots validated**: every number in the prod chat screenshots is golden-exact. One nit fixed via skill context: "Notable longs" were actually NETS.
- **Pricing pack** (spec: `docs/superpowers/specs/2026-07-10-pricing-pack-design.md`): ReportLogistic carries `S.Dif`/`S.Fob dif` (62/62 populated, USc/lb vs NY KC), `S. Price`+`S. Unit` (29/62 fixed; units USC/LB / USD/KG / USD/MT), `S.Term`, `sFixDte`. Built: `Sale` price fields, `priceToUscLb` (1 USc/lb = 22.046226 USD/MT), parser capture + SMT-weighted merge in `aggregateSales`, pure `src/lib/pricing.ts` (`computePricing`: overall + soldGrade/client/deliveryMonth/fixMonth/postGrade dims, fixed vs PTBF, coverage — unpriced sales excluded and listed, never guessed), `price-analytics` tool in position-query, demo seed enriched 60/60 from the real export (`.lua/enrich_demo.ts`). **Parity [9]**: 12 checks vs independently hand-computed constants (overall dif 84.0236/72.4717, fix-month ladder, NESTRADE 144.5596) — all green, 9 sections exit 0. Sandbox verified: grinder answer (BOLD +27.79/+16.72, LIGHT +7.56/+0.01 USc/lb), P&L/cost-basis correctly declined. postGrade bucket weights are **bags** (`allocatedBags`), not MT.
- **Not answerable (by design, agent declines)**: purchase cost basis (P-side fields zero on unallocated rows), P&L/MTM (no market quotes in any export), price history. `S.Hedge Value` semantics (1 vs 4) unknown — unused; confirm with Ivo, along with dif-unit assumption (USc/lb) and one real postGrade answer.
- `Sucafina/` reference repo dropped into the workdir by user — gitignored + tsconfig-excluded.
- **Prod is one push behind again after this pack** → `lua push all --force`, deploy via `/lua-deploy`.

---

### Handover prompt (paste into a new session)

> Resume the **Position Assistant** Lua agent (Sucafina Kenya desk). Read `SESSION-2026-07-09.md` and `HANDOVER.md` in full first — don't re-derive anything documented there. State: all build tasks complete; engine + skills validated to exact golden parity (`npx tsx src/__tests__/parity.ts` → 8 sections, exit 0); sandbox end-to-end verified (net −4,850.21 exact, hedge view exact, what-if correct). **Production is stale: it predates the blend-matcher strategy-drift fix and the rolling-horizon fix (both real bugs) — first push (`lua push all --force`) and deploy via `/lua-deploy` (never bare `lua deploy`; a hook blocks it).** Then work the ops list in SESSION §7: LuaPop channel (interactive `lua channels`), morning-report recipient env vars, verify the chat-upload→CDN fileId path with a real attachment, and chase the Ivo dependencies (raw XBS export, cert workbooks, blend-assignment history, ambiguous yield-split confirmations). Commit task-wise, never add a Co-Authored-By trailer.
