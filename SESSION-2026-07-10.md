# Session log — 2026-07-10 (deploy, pricing pack, client & shipment analytics)

Companion to `HANDOVER.md` (domain model, formulas) and `SESSION-2026-07-09.md` (build history). This file records what THIS session did: shipped the stale fixes, then built two analytics packs on top of the daily SOL uploads — with the research that scoped them and the QC that validates them.

**Status (superseded by §11): all XBS step-1 work is DONE locally — recon (everything derivable reconciles to golden exactly), `parseXbsStock`, parity [12] (53 checks), and the upload-time coverage report wired into `ingest-stock-report`. Local main = `c8939a0`; user is pushing + promoting (staged also carries `421d972`, the cert tagged-stock rollup). §1–§6 below describe the earlier state of this day; §7–§11 the resumed sessions.**

---

## 1. What was done, in order

| # | What | Outcome |
|---|---|---|
| 1 | Shipped the stale bug fixes | Pushed v1.0.2/1.0.3; **user promoted** skills v1.0.3 + persona v2 (agent version v2). Blend-matcher strategy-drift fix + rolling-horizon fix now live. |
| 2 | Production bootstrap | Seeded reference data in prod (114 blends / 67 assumptions / 34 assignment keys); loaded demo day; verified net −4,850 & horizon 2025/12–2026/09 exact. Set `MORNING_REPORT_EMAIL=dev@luaimplementation.ai` in sandbox+production (interim recipient). |
| 3 | Validated the user's production screenshots | Every number exact vs engine (totals, 5 grade rows, offers, AB FAQ shorts). One nit: "Notable longs" were actually **nets** → persona rule added. |
| 4 | **Pricing pack** (v1.0.4, deployed by user) | `price-analytics` tool: SMT-weighted avg differentials (contract + FOB-equivalent, USc/lb vs NY KC), fixed vs price-to-be-fixed split, dims: soldGrade/postGrade/client/deliveryMonth/fixMonth. Demo seed enriched 60/60 with real prices. |
| 5 | **Client & shipment pack** (v1.0.6, staged) | `client-exposure` (counterparty ranking: contracts, SMT/bags, share, month ladder, grades, destinations, payment terms, trader) + `shipment-status` (booked / preshipment-only / vessel-assigned states, per-month ladder, vessel/voyage/POL→POD/ETD/ETA detail). |
| 6 | **Bulletproofing audit + hardening** (v1.0.7, staged) | Audited Ivo's real upload chain end-to-end; added upload-time coverage report + drift warnings to `ingest-logistics-report`. |

Specs: `docs/superpowers/specs/2026-07-10-pricing-pack-design.md`, `docs/superpowers/specs/2026-07-10-client-shipment-analytics-design.md`.

## 2. The research angles (how each pack was scoped)

The method, both times: **study the raw export first, hand-compute expected values independently (python over the TSV), only then design** — the engine must reproduce numbers derived without it.

**Pricing** — column census of `ReportLogistic20260618-IVO.xls` (184 columns) on the 62 unallocated rows:
- `S.Dif` / `S.Fob dif` populated **62/62** → differentials are the reliable "price level" (USc/lb vs NY KC). `S. Price` only 29/62 (rest = price-to-be-fixed); units mixed USC/LB ×55, USD/KG ×4, USD/MT ×3 → normalization needed (1 USc/lb = 22.046226 USD/MT).
- `S.Term` mixed (FOB/CIF/DAP…) — why contract dif ≠ FOB dif → **user decision: always show both, no headline**.
- Purchase-side price fields ALL ZERO on unallocated rows → cost basis impossible from this export (declared out of scope; agent declines).
- Golden BASE FILE fixture and demo seed carry no prices → validation constants hand-computed from the raw TSV; demo seed enriched by joining the real export on (saleCtr, month).

**Client/shipment** — status + coverage census:
- The export is **pre-filtered to '6-Sales Unallocated'** (62/62) — no shipped rows exist, so "shipment analytics" = *booking state of the forward book*, not voyage tracking.
- 100%-covered fields: `S.City`, `S.CodCountry`, `Payment term`, `Trader`; `S.Cert` 20/62 (EUDR flags).
- Booking-stage fields partial by nature: PreshipID 18, vessel/ETD 13, ETA 10 — **and `'0'` is the empty marker** on booking fields (caught when a naive count said 61/61 booked).
- The header repeats Vessel/ETD/ETA/POD/TransType for a second transport leg that is entirely empty → first-match column lookup is correct.
- Split contracts ride one sailing on several bookings (SSKE-103502: two preship IDs, one vessel) → merge joins IDs with ' / ', keeps earliest dates. This recovered the 17th booked contract during TDD.

## 3. What was made (files)

**Engine (pure, tsx-runnable, no Lua imports):**
- `src/lib/pricing.ts` — `computePricing(sales, {dimension, blends})`
- `src/lib/book.ts` — `computeClientExposure(sales)`, `computeShipmentStatus(sales)`
- `src/lib/units.ts` — `USD_MT_PER_USC_LB`, `priceToUscLb()`
- `src/lib/types.ts` — `Sale` price/destination/booking fields, `SaleBooking`
- `src/lib/parse.ts` — captures 16 more columns; SMT-weighted price merge + booking-aware merge in `aggregateSales`

**Skills/tools (26 primitives = 1 agent, 5 skills, 19 tools, 1 job):**
- `src/skills/query.skill.ts` — `price-analytics`, `client-exposure`, `shipment-status` (+ shared `snapshotSales` loader; context rules: both difs side-by-side, three booking states, never call net a long, decline cost-basis/P&L/containers/invoices)
- `src/skills/ingestion.skill.ts` — upload-time coverage report + drift warnings

**Data/tests:**
- `src/seed/demo.ts` — DEMO_SALES enriched 60/60 (prices + destinations + bookings) via `.lua/enrich_demo.ts` (regenerate-able)
- `src/__tests__/parity.ts` — sections **[9] Pricing** and **[10] Client & shipment** vs hand-computed constants

## 4. How to verify / QC / test

**Layer 1 — engine parity (always run first):**
```bash
npx tsx src/__tests__/parity.ts   # 10 sections, MUST exit 0
npx tsc --noEmit                  # clean
npx lua compile --ci              # 26 primitives
```
Key constants [9]: 61 aggregated sales, overall dif **84.0236** / FOB **72.4717** USc/lb, fixed 28/−639.81 MT flat 326.9371, PTBF 33/−2,268.0, 7-bucket fix-month ladder, NESTRADE 144.5596.
Key constants [10]: KONINKLUJKE 8/−864.0/29.7% (largest), booked **17/−528.12 MT**, vessel-assigned 12/−410.52, ladder 2026/06 = 9 booked/13 unbooked, ETDs 9× Jun + 3× Jul.

**Layer 2 — tool layer (sandbox):**
```bash
npx lua test skill --name load-demo-snapshot --input '{}' --json
npx lua test skill --name price-analytics --input '{"dimension":"postGrade","grade":"grinder"}' --json
  # expect: BOLD dif 27.7888 / FOB 16.7164; LIGHT 7.5566 / 0.0094 (allocatedBags, not MT)
npx lua test skill --name client-exposure --input '{"top":3}' --json
  # expect: KONINKLUJKE −864 / 30.2% of demo book
npx lua test skill --name shipment-status --input '{"month":"2026/06"}' --json
  # expect: 9 booked (−244.92) / 13 unbooked (−134.25); SSKE-103502 with joined booking IDs
```

**Layer 3 — conversational QA (sandbox, then production after promote):**
- "At what price level am I short on average Grinders" → both difs side by side, PTBF share, no advice.
- "who am I most short to right now?" → KONINKLUJKE ~30%, with destination/payment context.
- "what's booked to ship in June? anything booked but without a vessel yet?" → three states kept distinct; SSKE-107744 (SINJYCDINC) must be listed as **preshipment-only, booked** — this was a real QA catch, the agent used to call it unbooked.
- Guardrails: "what's my P&L if the market rallies / cost basis / which container / has it been invoiced" → accurate declines naming what the data lacks.

**Layer 4 — the real-upload test (STILL PENDING, the one untested inch):**
Once the LuaPop channel exists, upload `demo-files/ReportLogistic_2026-06-18.xls` in chat with "ingest this, position date 2026-06-18". The new coverage block must report: 61 sales, 61 priced, 17 booked, 12 vessel-assigned, no unknown price units. This validates the chat-upload → CDN → bytes link, the only part parity can't reach.

**QC posture (why it's trustworthy):** columns are found by header name (reorder-proof; missing column throws loudly); the tools degrade honestly (unpriced sales excluded AND listed, absent fields declined not approximated); the same parse functions run in parity and production; fields survive assign-blends/confirm-blend (spread/in-place) and the Data-API round trip (proven in sandbox). Induced-from-one-export assumptions ('0'-as-empty, dif in USc/lb, empty second leg) are now monitored by upload-time warnings.

## 5. What else we can do (backlog, in rough value order)

1. **Promote v1.0.7** (client/shipment pack + coverage hardening) — everything validated, waiting on user.
2. **LuaPop channel + real-upload test** (Layer 4 above) — unlocks Ivo actually using it and closes the last verification gap.
3. **Fixed-vs-PTBF by grade/month** — "how much of my grinder book re-rates if NY rallies" — engine already computes the split; needs only a dimension parameter.
4. **Cert/EUDR exposure** — `S.Cert` (20/62) + DNP `certification` column: partial cert answers without waiting for the 3 external workbooks.
5. **Warehouse & crop-year splits** — DNP carries `WHSE` and `CropYear` (unparsed today).
6. **Morning report enrichment** — fold headline dif level + booked/unbooked ladder into the 06:00 report.
7. **Ivo dependencies (unchanged)** — raw XBS export, 3 cert workbooks + SOL futures export, blend-assignment history, yield-split confirmations; **new questions**: dif always USc/lb? sanity-check one blend-weighted postGrade price answer; `S.Hedge Value` 1 vs 4 semantics.
8. **Watch item** — snapshot document size if the real book is ≫100 contracts (60 is trivial today).

**Not answerable by design (agent declines, correctly):** purchase cost basis, P&L/mark-to-market (no market quotes in any export), price history beyond ingested snapshots, B/L/containers/invoices/warehouses on sales, trade advice.

## 6. Deploy checklist (when ready)

```bash
lua deploy all --force            # promotes staged v1.0.7 + persona v6
lua version create -m "client/shipment analytics + ingest coverage"
lua version promote v4            # (next version number)
```
Then production spot-checks: "who am I most short to?" and the June booking question above.

---

## 7. 2026-07-10 (resumed session): fixed-vs-PTBF split per bucket (backlog §5.3 — DONE)

Every `price-analytics` bucket (any dimension) now carries its own `fixed: {contracts, smt, flatUscLb}` / `ptbf: {contracts, smt}` split — answers "how much of my grinder book / my August book re-rates if NY rallies". postGrade buckets report the split in `allocatedBags`. Engine: `computePricing` refactored to one `Group` accumulator (all/fixed/ptbf) used for overall AND per-bucket; `PricingResult.overall` is now just a `PriceBucket` (split fields moved into `PriceBucket` itself). Persona context rule added: quote the bucket's PTBF volume + share; fixed volume does not re-rate.

Method as prescribed (§2): hand-computed constants in python over the raw TSV first (they cross-sum to the validated overall −639.81 fixed / −2,268.0 PTBF), TDD'd parity checks (watched them fail), then implemented. New parity constants (real export, per soldGrade — [fixed n, fixed SMT, flat USc/lb, ptbf n, ptbf SMT]): AA [11, −177.42, 362.0995, 12, −748.8]; AB [9, −259.14, 326.1024, 14, −876.0]; GRINDER [2, −151.2, 301.3071, 5, −583.2] (**79.4% PTBF**); MH [0, 0, —, 1, −38.4]. Delivery-month ladder: 2026/06 = 19 fixed/3 PTBF; **2026/08 onward = 100% PTBF** (Aug 9 ctr/−621.6, the deferreds are the re-rating risk). Verified: parity 67 checks exit 0, tsc clean, 26 primitives compile, sandbox tool tests (soldGrade + postGrade grinder), 2-turn chat QA exact vs constants. Pushed to server (staged; persona v8, backup v8). **Production still v3 — NOT promoted.**

## 8. 2026-07-10 (resumed session): cert/EUDR exposure (backlog §5.4 — DONE)

New `cert-exposure` tool (27th primitive; 20 tools now) + `src/lib/cert.ts` `computeCertExposure(sales, dnp)`. Sales side from `S.Cert` (already parsed); stock side from the DNP `certification` column (newly captured in `parseDailyNetPosition` + `DnpRow.certification`; `''` = untagged, field ABSENT = legacy row). Stock filter = unsold purchase rows (`0-In Store Origin unsold`, `2-To Be Shipped Unsold`, `P.MT` ≠ 0 — same states the workbook's Direct Sales Stock uses). EUDR-flagged = tag contains `EUDR`. Honesty rules baked into engine, tool caveats, AND persona context: **UNTAGGED = unknown, never "not certified"; figures are floors**; snapshots whose DNP predates capture get `stock: null` + a re-ingest note instead of a fabricated 0%-certified answer. `DEMO_DNP` regenerated with cert tags via `.lua/regen_demo_dnp.ts` (114/459 tagged).

Hand-computed parity constants (real exports, scratchpad `cert_census.py`; parity section [11], 5 checks): sales byTag [ctr, SMT]: AAA.EUDR [5, −280.8], CP.EUDR [3, −326.4], EUDR [2, −307.2], RA.EUDR [5, −302.4], RA [3, −172.8], 4C.RFA [1, −151.2], UNTAGGED [42, −1367.01]; **sales EUDR-flagged 15 ctr / −1,216.8 SMT (41.8%)**. Stock byTag [rows, MT]: RA [9, 123.22], RFA [19, 9.09], CP [2, 0.36], CP.EUDR [1, 38.4], EUDR [1, 21.49], UNTAGGED [153, 384.93]; total 185 rows / **577.49 MT unsold, EUDR-flagged 59.89 MT (10.4%)**. Verified: parity exit 0, tsc clean, 27 primitives compile, sandbox tool tests (legacy snapshot → honest stock decline; reloaded demo → stock constants exact), 2-turn chat QA incl. the "so the rest is non-certified?" guardrail (correctly refused). Pushed (staged; persona v9). **Production still v3 — NOT promoted.**

---

## 9. 2026-07-10 (resumed session): REAL raw XBS export arrived — reconciles to golden exactly

`forecast-context/XBS - Current Stock -ivo-2026-06-18 (2).csv` appeared in the working tree this session (untracked; now committed). This is **HANDOVER §9 data ask #1 delivered**: the real XBS Current Stock export for the golden day. Recon census (scratchpad `xbs_census.py`, 808 rows):

- **Total Qty = 2,134,097.3 kg = 35,568.3 bag-equivalents @60 kg — matches the golden theoretical total (Summary!C 35,568.29 bags) and the golden Stock hedge line (2,134.0974 MT) to the decimal.** So the workbook's stock total is XBS `Qty.` (kg)/60, NOT the raw `Bags` count (31,332 — bags aren't exactly 60 kg).
- Schema (33 cols): `Item Name` (raw grade: AA 13,769 bags, AB 4,556, ABOVE SCREEN 17 3,165, GRINDER B 2,941…), `Position Strategy Allocation` (PRE/IN/POST strategy tags — the stockcounter's input vocabulary), `Item Phase` (Intermediate 607 / Finished Good 164 / Scrap 37), `Inventory Type` (crop year, 775/808 = 2025/26), `Warehouse` (KAHAWA BORA 680), `Blocked` (68 rows), `Certification` (**different vocabulary from SOL**: "RAINFOREST ALLIANCE", "FAIRTRADE", "RFA,AAA" — 41/808 tagged), WIP rows (54) have no warehouse.
- **Next big task (new top backlog item):** parse this real format → stockcounter → per-grade theoretical parity vs golden Summary!C (census + hand-computed constants per grade first, as always). Would replace the synthesized `demo-files/XBS_Stock_2026-06-18.xlsx` story with the real thing, and unlock warehouse/crop-year/blocked splits + a second cert source.

## 10. 2026-07-10 (resumed session): DEPLOYED to production (agent v4) + verified

**User promoted agent v4** = skills v1.0.11 (27 primitives incl. `cert-exposure`) + persona v10. (Transcript detour worth knowing: an intermediate `push`/`deploy` picked up a stale v1.0.5 and was promoted as v3-era content; the follow-up `lua push all --force` → `lua deploy all --force` → `lua version create -m "new tools, cert and what not..."` → `promote v4` got the real 27-primitive build live.)

**Production spot-checks — all pass** (thread `prod-verify-0710`, demo day reloaded first so the snapshot carries cert tags):

| Check | Expected | Result |
|---|---|---|
| Demo day net + offers | −4,850 bags; golden offers table | exact ✅ |
| "Who am I most short to?" | KONINKLIJKE 8 ctr / 30.2% | exact (−14,400 bags) ✅ |
| June bookings, 3 states | 9 booked (−244.92) / 13 unbooked (−134.25); SSKE-107744 = preshipment-only BOOKED | exact ✅ |
| Grinder re-rate (new §7 tool) | BOLD 8/−7,984 + LIGHT 3/−4,511 bags PTBF (~87%) | exact ✅ |
| EUDR + guardrail (new §8 tool) | 15 ctr / −1,216.8 SMT; refuse "untagged = non-certified" | numbers exact, refusal clean ✅ |

**Prod QA catch → fixed + STAGED (rides the next promote):** the agent conflated "EUDR rows" with "tagged rows" on the stock side (said "2 of 185 rows carry any cert tag"; truth: **32 tagged / 192.56 MT**, of which 2 EUDR) and framed shares as of-the-tagged-set when `sharePct` is of the total. Root cause: the tool exposed no tagged-stock rollup, so the model guessed. Fix (commit `421d972`, TDD): `stock.tagged {rows, mt}` in `computeCertExposure` + parity check (32/192.56) + two new tool caveats (tagged-stock coverage line; "sharePct is of the TOTAL volume"). Pushed to staging — **staged is now ahead of production v4 by exactly this fix.**

**Scope confirmation (user asked):** the design doc (`forecast-context/sucafina_solution_design_document.md`, Position Analysis / agent #4) defines Ivo's inputs as **"XBS stock report + two logistics/position reports"** — exactly our three files (XBS Current Stock, ReportLogistic, DailyNetPosition), as **manual daily exports** (SOL "no API; manual daily export"; XBS "no API/cube; VPN; manual download"). Brian's Azure DB mirror of SOL/XBS is the phase-2 data source (hence HANDOVER's data-source adapter); live SOL/XBS integration is phase 2, blocked by no API. Acceptance signal per the doc: agent's net-position-by-date matches Ivo's manual morning reconciliation for a live day + one representative what-if correct.

---

## 11. 2026-07-10 (resumed session): XBS step-1 recon — everything derivable reconciles EXACTLY; the gap is Ivo's manual percentages (recoverable)

Read-only recon of the real raw XBS export against golden, per the prescribed method (scratchpad `xbs_recon.py` / `xbs_recon_final.py`: python reimplementation of the `stockcounter.ts` pipeline, independent of the TS engine). **Key discovery: the golden workbook's `Stocks` sheet carries the counter's actual output** — per-grade Already-in-POST (col I), Expected (col J), Total (col K) — plus a "BRILLIAN's MODEL" comparison table in cols M–Q (ignore; DIFFs are large, it's the alternative model the design doc mentions). So Summary!C is now decomposable layer by layer.

**Reconciles EXACTLY (raw CSV → counter logic, zero delta):**
- Grand total **35,568.29 bags** (Qty kg / 60) — reconfirmed.
- **FINISHED 5,783.01** (strategy prefix `FINISHED <x>` variants: AB - PLUS 2,174.33, AB - FAQ 1,830.65, ABC - FAQ 1,105.19, SPECIALTY - WASHED 424.85, AA - TOP 147.98, AA - FAQ 100.00).
- **Unclassified/Pending 951.80** = blank strategy 773.67 (UNCLASSIFIED) + non-PRE/IN/POST/FINISHED tags 178.14 (PENDING: `DUST` 172.66, `STONES` 2.15, `MBUNIS` 1.03, `Specialty - Washed` 2.28 — note mixed case).
- **Already-in-POST: 22/22 golden grade rows exact** (parity-[12] constants): SPECIALTY WASHED 461.67, 17 UP TOP 495.42, 16 TOP 8.40, 17 UP PLUS 3,591.81, 16 PLUS 187.96, 14 PLUS 0.12, PB - PLUS 3.97, 17 UP FAQ 2,234.54 (= 2,231.69 + FAQ MINUS 2.85 via consolidation map ✓), 16 FAQ 1,259.60, 14 FAQ 511.95, PB - FAQ 184.01, GRINDER BOLD 2,113.68, GRINDER LIGHT 0.75, MH 8.39, REJECTS P 304.06; all other grades 0. POST bucket total 11,687.76 ✓.
- **Vocabulary bridges (Summary!C vs counter grades):** Summary `POST NATURAL` 3,158.01 = counter `POST NATURAL` 2,696.34 (all expected) + `POST SPECIALTY WASHED` 461.67. Summary `POST MH` 348.81 = counter `POST MH` 27.37 + **`POST MBUNI HEAVY` 321.43**, carried in the sheet as a "Recent Purchase" +321.44 on the MH row with a `Check −321.44` cell (MBUNI HEAVY has no row of its own in the golden grade table but IS in the already TOTAL).
- **Expected pool = PRE+IN pool exactly: 17,145.72 bags** (PRE 10,341.79 + IN 6,803.93). Ivo forecasts 100% of PRE/IN into POST grades — no leakage, no haircut.
- Status buckets (bags): PRE 10,341.79 / IN 6,803.93 / POST 11,687.76 / FINISHED 5,783.01 / PENDING 178.14 / UNCLASSIFIED 773.67.

**Embedded Ivo questions §9 — ANSWERED by the recon** (golden total reconciles exactly *with them included*): Blocked=Yes rows (68, 338.55 bags), WIP/no-warehouse rows (54, 9,495.77 bags), old crop years (17, 102.40 bags) **all count**. For parity/ingestion: include everything; Blocked/warehouse/crop-year are analytics dimensions, not filters.

**What does NOT auto-reconcile — the per-grade Expected split.** It comes from percentages Ivo types into the counter UI per (strategy | batch-prefix) matrix row, persisted **only in his browser: `localStorage['processingPercentages']`** (confirmed in `new_stockcounter.html`). → New top data ask: have Ivo run `copy(localStorage.getItem('processingPercentages'))` in DevTools on the counter page and paste the result. With that JSON, the whole Expected column should reproduce mechanically.

Our conservative assumptions-based derivation (`deriveForecastPercentages` logic: assumptions.json + strategy_mapping.json + batch_mappings.json + `DEFAULT_STRATEGY_TO_POST`) resolves **6,381.37 of 17,145.72 bags (37%)**; where it does resolve it is close but not equal to Ivo (e.g. 16 PLUS +138.05, PB - PLUS +7.48, PB - TOP: we forecast 43.59, Ivo forecast 0). Unresolved 10,764.35 bags by reason:
- **no-grade 6,947.4 bags** — real item names unknown to `extractGrade`/assumption keys: `ABOVE SCREEN 17` (all `|RG` groups), `ABOVE SCREEN 14`, `BELOW SCREEN 17`, `BELOW SCREEN 16`, `Rejects L`, `Rejects B`, `REJECTS-REJECTS L/B`, `Elevator Balance`, `CHIPPINGS`, `GRINDER B`; plus mixed-grade matrix rows (e.g. `PRE SPECIALTY - WASHED|20` mixes AA/AB/PB/NH; `PRE GRINDER BOLD|20` mixes 8 grades) — Ivo's UI is per-row so mixing is fine for him, but assumption lookup needs a grade; fix would be per-raw-row (kg-weighted) derivation instead of per-group.
- **no-standard-strategy 2,214.2 bags** — `GRINDER RECOVERABLE` (1,561.98 + 549.60 bags) and `GRINDER LIGHT` (90.69 + 11.88) missing from strategy_mapping.json.
- **ambiguous-outputs 1,602.8 bags** — the known `DEFAULT_STRATEGY_TO_POST` nulls (SPECIALTY, GRINDERS, MBUNIS, REJECTS, ABC splits) we refuse to guess.

**Ivo questions from this recon (supersede/extend §9's):** (1) export `processingPercentages` (the one-liner above) — unlocks full Expected reproduction; (2) failing that: grade map for the raw item-name spellings above + strategy mapping for GRINDER RECOVERABLE / GRINDER LIGHT + the five ambiguous POST splits; (3) confirm Summary folds SPECIALTY WASHED→NATURAL and MBUNI HEAVY→MH permanently (or is the −321.44 Check a one-off manual patch?); (4) XBS↔SOL cert vocabulary map (§9, still open).

**Implication for `parseXbsStock` + parity [12]:** the parser and counter port are validated against real data today — parity can assert total, FINISHED, pending split, status buckets, and all 22 already-in-POST constants without any new inputs. The Expected side ships as: reproduce-with-percentages (once Ivo exports them) + honest unresolved report (the tool already has the `unresolved` pattern).

**DONE (same session, TDD):** `parseXbsStock` in `src/sources/UploadedFileSource.ts` (renamed from `parseStockWorkbook`; header-name-based so the real CSV and the synthesized demo .xlsx go through the same function) + parity section **[12]** (38 assertions, RED-verified then GREEN). Parser now also captures `blocked` (Yes→true), `itemPhase`, `cropYear` (from "Inventory Type"), `certification` (XBS vocabulary), parses `01-DEC-2024`-style intake dates deterministically (UTC), and strips thousands separators in Qty. `StockRow` extended accordingly. Parity is now **12 sections**; all three gates green (parity exit 0, tsc clean, 27 primitives).

**DONE (same session, TDD): XBS upload-time coverage report.** New pure `computeStockCoverage(rows)` in `src/lib/stockcoverage.ts` (15 more parity checks under [12]): rollups for blocked (68 / 338.55 bags), WIP-no-warehouse (54 / 9,495.77), crop years, XBS cert tags (41 / 1,139.32 bags), intake-date parse counts, pending strategy tags (4 tags / 178.135 bags — exactly on the 2dp boundary, asserted at 3dp), extra POST grades, zero-qty rows — plus **drift-only warnings** (novel pending tags, novel POST grades, zero-qty rows, warehoused rows without dates). The golden-day export is the asserted zero-warning baseline; injected drift rows must raise exactly 2 warnings. `ingest-stock-report` now returns the coverage block + warnings (mirrors the logistics pattern) and persists `stock.coverage` in the snapshot so blocked/crop-year/cert analytics can run later without raw rows (absent on older snapshots → the honest-decline pattern applies). Tool description + skill context updated (.csv accepted; warnings relayed verbatim; blocked/WIP/old-crop ALL COUNT per §11). Gates green.

---

## 12. 2026-07-10 (resumed session): stock-analytics tool (work-queue item 1 — DONE) + UTC intake-date fix

**`stock-analytics` (28th primitive; 21 tools)** — one query tool, one flat dimension per call (`warehouse | cropYear | blocked | cert`), backed by new pure `computeStockAnalytics(dimension, {location, coverage})` in `src/lib/stockanalytics.ts` reading exactly what `ingest-stock-report` persists. Method as prescribed: hand-computed census in python over the raw CSV first (scratchpad `stock_analytics_census.py`), parity section **[13]** RED-verified (33 assertion failures) then GREEN. Constants (golden day): 7 warehouses (KAHAWA BORA 25,661.26 bags / avgDays 48.05 / 72.15%; NO WAREHOUSE 9,495.77 / 26.70%; BOLLORE 251.88 / avgDays 411.72; MITCHELL COTTS 91.38 / **59.25 — beware double-rounding the census's 4dp print, true value 59.25497**; KPCU 43.33; TATU CITY 22.17; KENBELT 2.48); crop years 775/16/17 rows = 35,333.78/132.11/102.40 bags (99.34/0.37/0.29%, sum = total exactly); blocked 68/338.55/0.95% + notBlocked 740/35,229.74 (identity asserted); cert tagged 41/1,139.32/3.20%, untagged 767/34,428.97, byTag RAINFOREST ALLIANCE 28/832.71/2.34%, "RAINFOREST ALLIANCE, FAIRTRADE" 8/181.74/0.51%, FAIRTRADE 4/122.97/0.35%, "RFA,AAA" 1/1.90/0.01%. Honesty rules in tool caveats + query-skill context: carve-outs never exclusions; XBS≠SOL cert vocab, no merging; untagged = UNKNOWN; sharePct of TOTAL; coverage-dependent dims return null on pre-coverage snapshots → honest re-ingest note (warehouse still works — location summary is older).

**Bug found by the parity section — `parseIntakeDate` was timezone-dependent** (commit `7e14307`): XLSX (`cellDates`) hands back LOCAL-midnight Dates for the CSV's "01-DEC-2024" strings (verified: `2024-11-30T18:30:00Z` on IST), so the documented UTC regex path never ran and every warehouse's avgDays drifted +0.2292 days (5h30m) vs the hand-computed UTC census — and would have differed between laptop and Lua's servers. Fix: re-anchor Date instances to UTC midnight of their calendar date. This is why [13]'s avgDays asserts exist.

**DEMO_STOCK seed** (rides commit `c03ba84`): the demo snapshot previously carried NO `stock` block (theoretical pre-computed), so stock-analytics had nothing to read on the demo day. New `.lua/regen_demo_stock.ts` (untracked, like its `regen_demo_dnp.ts` sibling) generates `DEMO_STOCK` in `src/seed/demo.ts` from the committed real CSV — the exact block ingest persists (location/status/matrix/postBags/groups/rowCount/coverage) — and `load-demo-snapshot` now persists it. Parity [13] asserts the seed (808 rows, zero warnings, cert 41/1,139.32 via the engine).

**QA — all pass:** parity **13 sections / 137 checks exit 0**; tsc clean; **28 primitives** compile; sandbox tool tests all 4 dimensions exact vs constants (incl. reloaded demo); 3-turn chat QA (thread `qa-stock-analytics-0710`, sandbox): blocked answer led with total-then-carve-out ("35,568 bags, of which 339 blocked (0.95%)"), guardrail probe "so blocked is excluded?" → **"No — the opposite… included… of-which carve-out"**, cert answer gave floors + untagged-is-unknown + XBS≠SOL warning unprompted. First chat turn hit a transient ECONNRESET mid-skill-push (only 4/5 skills landed) — the agent answered from stale skills; retry re-pushed 5/5 and behaved. Commits `7e14307` + `c03ba84`, **pushed to staging — staged is now ahead of production v4 by these two plus `421d972`/`7a94644`/`c8939a0`; promote is yours.**

---

## 13. 2026-07-10 (resumed session): v5 PROMOTED + password-gated web frontend (`dashboard/`)

**Production is now agent v5** ("XBS thingy updated") — user ran `lua push all --force` → `lua deploy all --force` → `lua version create` → promote. Live build = 28 primitives (incl. `stock-analytics`), skills v1.0.14, persona v13. Everything §12 flagged as staged is deployed.

**`dashboard/` — the desk frontend** (commits `4a1e264`, `db88546`; supersedes a one-file `widget/` page built and rejected the same day). Vite + React + Tailwind, design language copied from `Sucafina/dashboard-v2` (same tokens: 13px Inter, true-neutral grays, single indigo accent 226/70/55, hairline borders, light-pinned per their client feedback; same collapsible sidebar / header patterns).

- **Shell = workspace sections**: sidebar `NAV_ITEMS` + react-router routes; only section today is **Position Agent** (`/position`, everything else redirects there). A future section = one NAV_ITEMS entry + one `<Route>`.
- **Multi-chat with NO backend** (LuaPop bundle recon, verified in the UMD source): `setupSessionId(config)` prefers `config.sessionId` over localStorage — so each sidebar chat is a UUID we mint, passed via init config; the widget resumes that thread server-side. Chat LIST (id/title/timestamps) in localStorage (`position-assistant-chats`); delete only forgets the list entry.
- **Widget isolation**: LuaPop runs in an `srcdoc` iframe (dashboard-v2's discovery — the ~22 MB bundle patches `history`, opens sockets, starts intervals). Remount with a new `sessionId` = switch conversation.
- **Auto-titling**: bundle's `emitWidgetEvent` posts full payloads (`data.content`) only to its OWN window; the parent copy is stripped, and `message_sent` never carries text. So the iframe relays events up and the first ASSISTANT reply titles an untitled chat (inline rename always available).
- **Password gate**: Vercel Edge Middleware adapted from dashboard-v2 — `SITE_PASSWORD` env (no gate if unset), sha256 cookie `position_assistant_auth` (salt `position-gate::`), branded Sucafina login page, 7-day cookie. `vite dev` is ungated.
- **Chat talks to PRODUCTION** (`environment: 'production'`, agent id via `VITE_LUA_AGENT_ID`, baked fallback). Attachments enabled — the deployed UI is the natural vehicle for the chat-upload test.

**Verified**: typecheck + prod build green (~200 KB app JS), dev server on `:5175` serves. **NOT yet verified (needs a human in the browser)**: the widget handshake end-to-end — send a message, New chat, switch back, history resumes; and the chat-upload test. **Not yet deployed to Vercel.** Deploy: project with Root Directory `dashboard/` + `SITE_PASSWORD` env (steps in `dashboard/README.md`).

---

## 14. 2026-07-10 (resumed session): spreadsheet-intake preprocessor (chat uploads fixed) + v6 PROMOTED

**Why uploads were broken** (user report: "can't upload/process excel/csv"): the widget and the ingest tools were both fine — the failure was the layer between. A spreadsheet attachment forwarded to the model as a raw file part is rejected by Anthropic (`AI_UnsupportedFunctionalityError` on spreadsheet media types), aborting the whole turn with an empty reply. Root cause + the fix pattern were already verified 2026-07-07 in the `file-types/` reference agent (`file-types/file-support.md`); that folder is now gitignored (reference-only, like `Sucafina/`).

**`spreadsheet-intake` PreProcessor (29th primitive)** — `src/processors/spreadsheetIntake.ts`, ported from the reference and made desk-aware. Intercepts csv/xls/xlsx file parts (media type or URL extension) BEFORE the model: fetches bytes (URL or base64), sniffs first (don't CDN-store unparseable files), `CDN.upload`, then REPLACES the file part with a text manifest. New pure `src/lib/exportSniff.ts` classifies WHICH desk export it is using the same header signatures the parsers key on — XBS stock: "Position Strategy Allocation"+"Qty."; SOL DNP: "Quality"+"TotLine"; SOL logistics: "Sale Ctr."+"S.Ship." — across all real containers (xlsx ZIP, CFB binary, UTF-16LE TSV named .xls, quoted raw CSV; text that isn't TSV goes through SheetJS so quoted CSV parses correctly). The manifest names the right ingest tool ("detected: XBS Current Stock export, N data rows. Call ingest-stock-report with this fileId"); unknown spreadsheets get their headers listed + an explicit "ask the trader before ingesting". **Manifest format contract**: `fileId=<id>;` — `resolveFileId` (skills/store.ts) gained a regex fallback over text parts, so "ingest the file I just uploaded" resolves even without an explicit id (raw file-part scan kept). Ingestion skill context documents the manifest. Compute path untouched — the tools read the same CDN bytes through the same parsers.

**QA**: new `src/__tests__/sniff.ts` (`npx tsx`, same convention as parity) — all three real 2026-06-18 demo exports classify correctly (48/459/62 data rows), synthesized quoted-CSV XBS → `xbs-stock`, generic CSV → `unknown`, manifest↔regex round-trip. Parity 13 sections / 137 checks still exit 0; tsc clean; compile now **29 primitives**.

**Production is now agent v6** ("updated excel config for anthropic") — user ran push → `lua deploy all --force` → `lua version create` → promote. Live: skills v1.0.16, `spreadsheet-intake` v1.0.2, persona v15. ⚠️ **Source backup failed after the batch push** (`fetch failed` — primitives ARE live, do NOT re-push): recover with `lua push backup --force --fresh`; until then `lua init` for this agent restores an older snapshot.

**Dashboard**: dev server re-verified on `:5175`. Upload flow is now widget → preprocessor → CDN → manifest → ingest tool; the §13 work queue's chat-upload test (61 sales / 808-row XBS, zero warnings) should now additionally show the manifest turn first. Vercel deploy + Lua-dashboard link hookup = user's next step.

---

## 15. 2026-07-10 (resumed session): dashboard on Vercel + Twenty redesign + the REAL upload blocker found (widget-side)

**Deployed**: `https://position-agent.vercel.app` (user-driven). `SITE_PASSWORD` env gates it (no gate if unset — flagged to user); domain whitelisted in the Lua admin dashboard (origin-level entry is the one that matters; `environment: 'production'` in the widget config bypasses the whitelist anyway).

**Twenty CRM design retune** (commit `3d9b79d`, calibrated against `twenty-ui` theme constants fetched from the repo — GrayScale/Background/Border/Font/BoxShadow/Text): the miss wasn't tokens but **layout physics** — now a gray canvas with all content floating in ONE rounded hairline-bordered white panel (`shadow-panel`), transparent sidebar, breadcrumb bar inside the panel, white bordered buttons (accent never fills buttons — the old solid-indigo "New chat" slab is gone), neutral 16px stroke-1.6 icons, 11px sentence-case section labels, 28px rows, text #333/#666/#999 + borders #EBEBEB/#D6D6D6 (Twenty's exact gray12/11/9, gray5/6).

**Uploads were blocked in the WIDGET, not the platform** (user screenshot: csv/xls greyed out in the macOS picker; commit `c2119c3`): the UMD bundle hardcodes `accept: "image/*,video/*,audio/*,.pdf,.doc,.docx,.txt"` (no spreadsheets), AND its `matchesAccept()` compares `file.type === pattern` — MIME vs extension patterns like `.pdf` never match, so EVERY non-media file (PDFs included) was rejected post-selection with "No files match the accepted types". Fix: the srcdoc iframe now **fetch-patches the bundle** (github.io sends `ACAO: *`): appends `.csv,.xls,.xlsx` to the accept string, rewrites the validator to also match extension patterns against `file.name`, loads via Blob URL. Exact-string patches — both verified to hit exactly once on the live bundle; if upstream changes they no-op with a console warning (and a script-src fallback if the fetch fails). So the full upload chain is now: picker (patched accept) → matchesAccept (patched) → chat upload → `spreadsheet-intake` preprocessor (§14) → CDN + manifest → ingest tools.

**Verified**: typecheck + prod build green; CORS preflight confirmed; patch simulation on the downloaded bundle (1 hit each). **Still needs the human browser pass**: the §13 work-queue upload test (ReportLogistic → 61 sales; XBS CSV → 808 rows, zero warnings) on the deployed URL after redeploy.

---

## 16. 2026-07-10 (resumed session): prod-test verification + sniff delimiter fix + query filters pack

**User tested production (threads ~01:50–03:28 UTC) and asked for verification + filters. Every number in the screenshots verified exact** vs the engine/golden: net-by-grade table, offer roll-ups, AB FAQ −5,844 bags recovery, grinder difs (27.7888/16.7164, 7.5566/0.0094), fixed/PTBF bags (1,800 / 12,495). Two agent claims were **model-side slips on truthful tool data**: "11 of 12 contracts" (bucket counts overlap; truth 9 distinct grinder contracts, 1 fixed / 8 PTBF) and calling AB FAQ "POST 16 FAQ" (it's 16 FAQ ×1 + 15 FAQ ×0.5 — numerically equal here since 15 FAQ horizon shorts = 0).

**Log findings:** (1) the 01:59/02:26 upload turns hard-failed with `AI_UnsupportedFunctionalityError` — they predate the v6 promote; the 03:20 upload went through `spreadsheet-intake` fine. (2) That upload's manifest said "not recognized" for the REAL XBS CSV: its header row embeds a literal tab (`Outturn / Factor\t,Warrant No`), so exportSniff's "first line has a tab → TSV" heuristic split a 33-column CSV into 2 tab-fields. **Fixed** (`765043e`): delimiter by count (tabs > commas); real CSV added to sniff.ts (RED-verified; classifies xbs-stock, 808 rows). (3) `query-position`'s month param was ignored without grade — the agent probed all 10 months, got identical payloads, and honestly declined; "demo data" labels are correct behavior (`demo: true` on the seeded snapshot — the only snapshot on file).

**Query filters pack** (`aa901e3`, method as prescribed — python hand-census of golden column sums first, parity [14] RED then GREEN, +15 checks):
- `monthTotals` (shorts.ts) + `resolveOfferQuery` (netposition.ts) + `distinctContractsForGrades` (pricing.ts). Golden by-month constants cross-sum to −40,418.5 over the horizon; found+kept the workbook-clipped 2024/10 straggler (SSWW-96188B, −20 bags POST NATURAL).
- `query-position`: month alone → shorts-by-grade for that month; full view → shortsByMonth ladder; grade accepts offer names (weighted roll-up with members + weighted by-month); horizon-clipping note on every view.
- `client-exposure` +month/+soldGrade; `cert-exposure` +client/+month (sales side only, caveated); `shipment-status` +state; `price-analytics` postGrade → `distinctContracts` + never-sum-buckets caveat. Skill context documents all of it.

**Verified:** parity 14 sections (137+17 checks) exit 0; sniff green; tsc clean (root tsconfig now excludes `dashboard/`+`file-types/`, `2eb95d5`); 29 primitives compile; sandbox tool tests exact (month view −6,319.5; AB FAQ weighted months match python; distinct 9/1/8; June preshipment-only = SSKE-107744). **Backup recovered** (`lua push backup --force --fresh` → v15) then full push staged (persona v16). **Production still v6 — promote is yours.** Proper seeding of real days stays: upload all three exports in chat (XBS CSV now auto-detected) or run the ingest tools; `demo: true` clears only when a real snapshot exists.

---

## 17. 2026-07-10 (resumed session): response-discipline pack — citation, no-guessing, no-narration (user mandate)

User mandate: proper citation ALWAYS, no value ever guessed, only offer what tools actually do, no "Let me…" thinking leakage (it leaked at 03:21 UTC: "Let me pull it."), verification always relayed.

**Schema audit first:** every declared tool param across all 5 skills is actually consumed (the §16 month-filter fix was the only advertised-but-dead param). Ingestion context already mandates relaying coverage/warnings verbatim.

**`src/lib/cite.ts`** (commit `f622661`, TDD parity [15]): pure `citeLine()` builds the provenance footer — `source: <tool> · snapshot <date> (DEMO seed, not live data) · <source exports> · ingested <ISO-minute>` — so citation is mechanical, never composed by the model. Attached as `cite` to every numeric tool result: all 7 query tools, the 3 ingest tools (+ load-demo-snapshot), compute-net-position, compute-futs-spread, compute-forward-sales, compute-theoretical-stock.

**Persona "Response discipline (HARD RULES)"** (5 rules): never guess a number (must exist in a tool result; never sum overlapping buckets — use distinctContracts); end every data answer with the `cite` line verbatim ("— " prefix); no intent narration ("Let me pull that"…) — reply starts with the answer; only offer documented tool params/fields; always relay demo flag / coverage / warnings / pending blends. Query-skill context repeats the cite rule.

**Verified:** parity 15 sections exit 0; sniff green; tsc clean; 29 primitives. Sandbox chat QA (3 probes): grinder price → demo-first, both difs, **distinctContracts 9/1/8 quoted correctly** (the §16 prod slip, now unmakeable), cite footer verbatim, zero narration; grinder by-month → horizon-clipping warning + fuzzy-resolution disclosure + cite; P&L-rally + client-warehouse cross-link → two honest declines naming what the data lacks, offering only real alternatives, correctly no cite (no numbers). Pushed to staging (persona v17). **Production still v6 — promote is yours.**

---

### Handover prompt (paste into a new session)

> Resume the **Position Assistant** Lua agent (Sucafina Kenya desk). **First gather context, then STOP and wait for my green light — do not start any work, commits, pushes, or chats until I say go.** Context-gathering: read `SESSION-2026-07-10.md` §12–§14 in full (stock-analytics, the `dashboard/` frontend, the spreadsheet-intake preprocessor), skim §11, then `HANDOVER.md` / earlier sections only as needed — don't re-derive anything documented there. Then run the state checks: `npx tsx src/__tests__/parity.ts` (**13 sections / 137 checks**, MUST exit 0), `npx tsx src/__tests__/sniff.ts` (all sniff checks pass), `npx tsc --noEmit` (clean), `npx lua compile --ci` (**29 primitives**; NOTE it rewrites `lua.skill.yaml` to the active deploy — `git checkout lua.skill.yaml` afterward if it dirties), `npx lua version list --ci` (expect **v6 active**), and in `dashboard/`: `npm run typecheck` + `npm run build` (both clean). Post the summary and WAIT.
>
> State: production = **agent v6** (29 primitives incl. `stock-analytics` + the `spreadsheet-intake` preprocessor, skills v1.0.16, persona v15). ⚠️ The v6 batch push's **source backup failed** (`fetch failed`) — primitives are live, do NOT re-push; recover with `lua push backup --force --fresh` (until then `lua init` restores an older snapshot). §12 shipped `stock-analytics` (four flat dimensions over persisted rollups; parity [13]; DEMO_STOCK seed from the real CSV; plus the `7e14307` UTC intake-date fix — avgDays was timezone-dependent). §13 shipped `dashboard/`: password-gated Vite+React frontend in the `Sucafina/dashboard-v2` design language — workspace-sections shell (only section: Position Agent at `/position`), chats panel INSIDE the section, multi-chat with no backend (each chat = a Lua session id passed to LuaPop's init; `setupSessionId` resumes the thread server-side; list in localStorage; auto-title from first assistant reply relayed out of the srcdoc iframe). Edge-middleware password gate (`SITE_PASSWORD`). §14 shipped the `spreadsheet-intake` preprocessor: chat spreadsheet uploads no longer abort the model turn — csv/xls/xlsx attachments are CDN-stored and replaced with a `fileId=<id>;` manifest that names the detected export (`lib/exportSniff.ts` signatures) and the right ingest tool; `resolveFileId` regex-scans manifests as fallback; sniff regression test at `src/__tests__/sniff.ts`. **Not yet done: backup recovery; Vercel deploy + Lua-dashboard link; in-browser verification of multi-chat resume; the chat-upload test.** I promote deploys myself (`lua deploy all --force` → `lua version create` → `lua version promote`); NEVER run bare `lua deploy` (hook blocks it; the hook also misfires on some multi-line Bash heredocs AND some `$VAR` interpolations — use `python3 -c` one-liners or scratchpad scripts). Working method for ANY new analytics: hand-compute expected constants independently (python over `forecast-context/`), then TDD a parity section before implementing — §2/§4/§7/§8/§11/§12 pattern.
>
> Work queue (after my green light, in order): **(1) Dashboard verification + deploy.** In-browser: send a message, New chat, switch back — history must resume (the `sessionId` handshake is code-verified against the UMD source but never watched end-to-end); then deploy to Vercel (project Root Directory = `dashboard/`, env `SITE_PASSWORD`, optional `VITE_LUA_AGENT_ID`; steps in `dashboard/README.md`; whitelist the domain in the Lua admin dashboard once stable). **(2) The real chat-upload test through the UI** (closes the only untested ingestion link, chat upload → CDN → bytes): upload `demo-files/ReportLogistic_2026-06-18.xls` → coverage must say 61 sales / 61 priced / 17 booked / 12 vessel-assigned; upload the real XBS CSV (`forecast-context/XBS - Current Stock -ivo-2026-06-18 (2).csv`) → 808 rows, ZERO warnings. **(3) Morning-report enrichment** (headline dif + booked/unbooked ladder in the 06:00 report). **(4) Ivo questions** — TOP ASK: the `processingPercentages` export one-liner (`copy(localStorage.getItem('processingPercentages'))` on the stock-counter page — unlocks reproducing the golden Expected column); fallbacks per §11 (grade map for ABOVE SCREEN 17 / BELOW SCREEN 17/16 / ABOVE SCREEN 14 / Rejects L/B / Elevator Balance / CHIPPINGS / GRINDER B; strategy mapping for GRINDER RECOVERABLE + GRINDER LIGHT; the five ambiguous POST splits); confirm the SPECIALTY-WASHED→NATURAL and MBUNI-HEAVY→MH folds are permanent (the −321.44 "Check" cell); XBS↔SOL cert map; plus the standing ones (dif always USc/lb?, sanity-check one blend-weighted postGrade price answer, `S.Hedge Value` 1 vs 4, HANDOVER §9 data asks) — do NOT email anyone without asking. House rules: commit task-wise, never add a Co-Authored-By trailer; `Sucafina/` dir is reference-only (gitignored, tsconfig-excluded); `LIGHTHOUSE-MCP.md` is untracked and not ours.
